あなたはITエキスパートです。

下記のように入力としてディレクトリがあたえられるので、ディレクトリ中やさらにその下の子供ディレクトリに存在する画像や動画を日付ごとに振り分ける処理を作ってください。

下記要件

# 入力

./rename-media-for-xiaomi.sh dirname

# 要件

- dirnameや更にその中のディレクトリのsubdir内すべての画像と動画に作用すること
- 拡張子から画像や動画を判断すること
- 振り分け方のルール
    - 画像(RW2,DMG,heic, png, jpegなど代表的な拡張子は基本対応すること)と動画(mp4,mkv,movなど代表的な拡張子は基本対応すること)の場合で下記のようにフォルダが分類されること

        ```terminal
        dirname
           -IMG
               -2025-06-01
                   -IMG_20250601_001000.heic
               -2025-06-02
                   -IMG_20250602_001000.RW2
           -VID
               -2025-06-01
                   -VID_20250601_001000.mp4
               -2025-06-02
                   -VID_20250602_001000_DOLBY.mp4
        ```

    - ファイル名フォーマットは下記の通り
        - (IMG|VID)_YYYYMMDD_hhmmss_{specialstring}_{number}.(extension)
            - {specialstring} ... 動画によっては、ファイル名に特殊な文字列が入るので、それをそのまま転記
            - {number} ... YYYYMMDD_hhmmssが同一のものが複数ある場合、インデックスとしてインクリメントする
            - (extension) ... 元ファイルの拡張子をそのまま
            - 例: IMG_20250601_001000_PORTRAIT.heic, IMG_20250601_001000_1.heic, VID_20250602_001000_DOLBY.mp4
    - また、基本的にファイル名から振り分けを行うが、ファイル名がフォーマットどおりでない場合は、EXIF情報の撮影日を参考にすること(FileCreateDate、FileModifyDateの順でEXIFを参照)
    - exifを参照する場合は、exiftoolを使用する。
- 動画や画像の量が多いのでパフォーマンス重視すること
